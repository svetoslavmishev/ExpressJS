<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Car Rental</title>
    <link href="/css/site.css" type="text/css" rel="stylesheet">
    <link href="/favicon.ico?" rel="shortcut icon"/>
</head>

<body>
    <div class="container">
        <header>
            <ul class="menu">
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/about">About</a>
                </li>
                <li>
                    <a href="/cars/all?page=0">Rent a car</a>
                </li>
                {{#if currentUser}}
                <li>
                    <a href="/users/{{currentUser.id}}">My cars</a>
                </li>
                {{/if}} 
                {{#if currentUser}} 
                {{#if currentUser.isAdmin}}
                <li>
                    <a href="/cars/add">Create Car</a>
                </li>
                {{/if}}
                <li>
                    <form id="logout-form" action="/logout" method="post"></form>
                    <a href="javascript:document.getElementById('logout-form').submit()">Logout</a>
                </li>
                <span>Welcome
                    <b class="shadow" ;>{{currentUser.username}}</b>!</span>
                {{else}}
                <li>
                    <a href="/login">Login</a>
                </li>
                <li>
                    <a href="/register">Register</a>
                </li>
                {{/if}}
            </ul>

        </header>
        <main>
            {{#if globalError}}
            <span>{{globalError}}</span>
            {{/if}} {{{body}}}

        </main>
    </div>

</body>

</html>